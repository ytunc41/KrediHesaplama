<Window x:Class="KrediHesaplama.WpfUygulama.AcilisEkrani"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Açılış" Height="450" Width="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border CornerRadius="20" Background="{StaticResource WindowBackgroundBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>
        <Grid>
             <!-- Logo/Icon (Circular Clip) -->
             <Border Width="300" Height="300" CornerRadius="150" HorizontalAlignment="Center" VerticalAlignment="Center">
                 <Border.Background>
                     <ImageBrush ImageSource="icon.png" Stretch="Uniform"/>
                 </Border.Background>
             </Border>
             
             <!-- Loading Area -->
             <StackPanel VerticalAlignment="Bottom" Margin="20,0,20,30">
                 <Grid Margin="0,0,0,5">
                     <TextBlock Text="Yükleniyor..." HorizontalAlignment="Left" Foreground="{StaticResource ForegroundBrush}" FontWeight="SemiBold"/>
                     <TextBlock x:Name="lblYuzde" Text="%0" HorizontalAlignment="Right" Foreground="{StaticResource ForegroundBrush}" FontWeight="SemiBold"/>
                 </Grid>
                 <ProgressBar x:Name="prgYukleniyor" Height="6" BorderThickness="0" Background="{StaticResource ControlBackgroundBrush}" Foreground="{StaticResource AccentColorBrush}" Maximum="100"/>
             </StackPanel>
        </Grid>
    </Border>
</Window>
